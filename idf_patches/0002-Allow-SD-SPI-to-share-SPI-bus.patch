From 7246f992177881fe84cfa1451a4957f177acdbea Mon Sep 17 00:00:00 2001
From: OtherCrashOverride <OtherCrashOverride@users.noreply.github.com>
Date: Fri, 29 Jun 2018 20:21:31 -0500
Subject: [PATCH 2/2] Allow SD-SPI to share SPI bus.

---
 components/driver/sdspi_host.c | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/components/driver/sdspi_host.c b/components/driver/sdspi_host.c
index d3e37489f..6e6cdba99 100644
--- a/components/driver/sdspi_host.c
+++ b/components/driver/sdspi_host.c
@@ -300,7 +300,7 @@ esp_err_t sdspi_host_init_slot(int slot, const sdspi_slot_config_t* slot_config)
             slot_config->dma_channel);
     if (ret != ESP_OK) {
         ESP_LOGD(TAG, "spi_bus_initialize failed with rc=0x%x", ret);
-        return ret;
+        //return ret;
     }
 
     // Attach the SD card to the SPI bus
-- 
2.17.1

